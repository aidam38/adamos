#/bin/bash

choice=$(echo "Shutdown\nZzz\nReboot" | dmenu -i) 
if [ "$choice" = "Shutdown" ]; then
	sudo poweroff now
if [ "$choice" = "Zzz" ]; then
	sudo zzz now
elif [ "$choice" = "Reboot" ]; then
	sudo reboot now
fi
