# Gmail configuration
set imap_user = "krivka.adam@gmail.com"
set imap_pass = "mewfvgycbihbywkm"
set folder = "imaps://imap.gmail.com:993"
set smtp_url = "smtp://krivka.adam@smtp.gmail.com:587"
set smtp_pass = "mewfvgycbihbywkm"
set spoolfile = "+INBOX"
set postponed = "+[GMail]/Drafts"
set header_cache = "~/.mutt/cache/headers"
set message_cachedir = "~/.mutt/cache/bodies"
set record = ""

# Basic settings
set sort = "reverse-date"
set editor = "$EDITOR"

# Formatting
set date_format = "%d/%m/%y %I:%M"
set index_format = "%D | %-15.15F | %s (%-4.4c)"

# Coloring
color index_author red default '.*'
color index_subject cyan default '.*' 
color index_date green default

# Bindings
bind index,pager i noop
bind index,pager g noop
bind index \Cf noop
bind editor <space> noop
bind index G last-entry
bind index gg first-entry
bind pager j next-line
bind pager k previous-line
macro pager J jjjjjjjjjj
macro pager K kkkkkkkkkk
bind pager gg top
bind pager G bottom
bind index d half-down
bind index s half-up
bind index D delete-message
bind index U undelete-message
bind index f search
set markers = no
set mark_old = no
set mime_forward = yes
